#!/bin/bash
# Convenience wrapper for prod-lens CLI from project root
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export PYTHONPATH="$SCRIPT_DIR/dev-agent-lens/scripts/src:$PYTHONPATH"
exec python3 -m prodlens.cli "$@"
