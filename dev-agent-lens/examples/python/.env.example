# Claude Code SDK Configuration
# Choose ONE authentication method:

# Option 1: API Key Authentication (basic_usage.py)
# ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
# ANTHROPIC_BASE_URL=http://localhost:8082

# Option 2: OAuth Authentication (basic_usage_oauth.py) 
ANTHROPIC_AUTH_TOKEN=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
ANTHROPIC_BASE_URL=http://localhost:4000

# Optional: Override the default model
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# OAuth Setup Instructions:
# 1. Generate OAuth token: bun run get-token.ts > credentials.json
# 2. Set OAuth token: export ANTHROPIC_AUTH_TOKEN=$(cat credentials.json | jq -r .accessToken)
# 3. Set OAuth base URL: export ANTHROPIC_BASE_URL=http://localhost:4000
# 4. Start proxy: docker compose up -d
# 5. Run OAuth example: uv run python basic_usage_oauth.py